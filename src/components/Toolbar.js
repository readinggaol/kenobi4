import Button from "./Button"

const Toolbar = () => {
    return (
        <div className="toolbar">
            <Button title="Test" />
            <Button />
        </div>
    )
}

export default Toolbar
